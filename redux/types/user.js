import { NS } from '../../constants/system';

// ----------------

const TYPE_NAME = 'USER';

// ----------------

export const RESTORE_BRANCH           = `${NS}/${TYPE_NAME}/RESTORE_BRANCH`;
export const SIGNUP_EMAIL_$NETWORK    = `${NS}/${TYPE_NAME}/SIGNUP_EMAIL_$NETWORK`;
export const SIGNIN_EMAIL_$NETWORK    = `${NS}/${TYPE_NAME}/SIGNIN_EMAIL_$NETWORK`;
export const FORGOT_PASSWORD_$NETWORK = `${NS}/${TYPE_NAME}/FORGOT_PASSWORD_$NETWORK`;
export const NEW_PASSWORD_$NETWORK    = `${NS}/${TYPE_NAME}/NEW_PASSWORD_$NETWORK`;
export const VERIFY_EMAIL_$NETWORK    = `${NS}/${TYPE_NAME}/VERIFY_EMAIL_$NETWORK`;
export const USER_$NETWORK            = `${NS}/${TYPE_NAME}/USER_$NETWORK`;
