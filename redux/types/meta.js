import { NS } from '../../constants/system';

// ----------------

const TYPE_NAME = 'META';

// ----------------

export const RESTORE_BRANCH         = `${NS}/${TYPE_NAME}/RESTORE_BRANCH`;
export const GET_LOCATIONS_$NETWORK = `${NS}/${TYPE_NAME}/GET_LOCATIONS_$NETWORK`;
