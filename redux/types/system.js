import { NS } from '../../constants/system';

// ----------------

const TYPE_NAME = 'SYSTEM';

// ----------------

export const RESTORE_STATE    = `${NS}/${TYPE_NAME}/RESTORE_STATE`;
export const START_LOAD_PAGE  = `${NS}/${TYPE_NAME}/START_LOAD_PAGE`;
export const FINISH_LOAD_PAGE = `${NS}/${TYPE_NAME}/FINISH_LOAD_PAGE`;
